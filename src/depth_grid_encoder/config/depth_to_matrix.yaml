depth_to_matrix:
  ros__parameters:
    depth_topic: "/camera/camera/depth/image_rect_raw"
    rows: 5
    cols: 10
    # z_min_m / z_max_m — definidos en nav_bringup/config/sensor_range.yaml
    #metric: "min"
    #smoothing_alpha: 0.0
